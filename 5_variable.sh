#!/bin/bash   #shebang or hashbang line to specify the interpreter, it runs the script using the Bash shell.


# name = "kashif"  # do not use spaces around the equal (=) sign

name="kashif" # Correct way to assign a variable in shell script
# Variables in shell scripts do not require a type declaration and can be assigned values directly

echo "Hello, $name"

age=30 
echo "You are $age years old."

favorite_color="blue"
echo "Your favorite color is $favorite_color."
